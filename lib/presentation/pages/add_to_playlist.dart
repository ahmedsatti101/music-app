import 'package:flutter/material.dart';

class AddToPlaylist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Playlist page', style: TextStyle(fontSize: 23))
    );
  }
}
